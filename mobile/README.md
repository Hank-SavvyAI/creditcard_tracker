# Credit Card Tracker - Mobile App (iOS/Android)

這是 Credit Card Tracker 的移動應用版本，使用 Capacitor 將 React/Next.js 網頁打包成原生 iOS 和 Android app。

## 🚀 快速開始

### 1. 安裝依賴

```bash
cd mobile
npm install
```

### 2. 開發模式（瀏覽器測試）

```bash
npm run dev
```

然後在瀏覽器打開 http://localhost:9001

### 3. 構建並同步到原生平台

```bash
# 構建 Next.js 靜態網站
npm run build

# 初始化 Capacitor (只需要第一次)
npx cap init

# 添加 iOS 平台
npm run cap:add:ios

# 同步代碼到 iOS
npm run cap:sync
```

### 4. 在 Xcode 中開啟並運行

```bash
npm run cap:open:ios
```

這會打開 Xcode，然後您可以：
1. 選擇模擬器或真機
2. 點擊運行按鈕 ▶️
3. App 就會安裝並運行在設備上

## 📱 功能特點

- ✅ **底部 Tab 導航** - iOS 標準的底部導航欄
- ✅ **完整的福利追蹤功能**
- ✅ **推播通知支持** (需要配置)
- ✅ **離線支持** (PWA)
- ✅ **原生體驗** - 使用 iOS/Android 原生功能

## 🛠️ 可用的腳本

```bash
# 開發模式
npm run dev

# 構建靜態網站
npm run build

# 添加 iOS 平台
npm run cap:add:ios

# 添加 Android 平台
npm run cap:add:android

# 同步代碼到原生平台
npm run cap:sync

# 在 Xcode 中打開 iOS 專案
npm run cap:open:ios

# 一鍵構建並打開 iOS
npm run ios:build

# 一鍵構建並打開 Android
npm run android:build
```

## 📂 專案結構

```
mobile/
├── src/
│   ├── app/                 # Next.js 頁面
│   │   ├── dashboard/       # 我的卡片
│   │   ├── cards/           # 探索卡片
│   │   └── profile/         # 個人設定
│   ├── components/          # React 組件
│   │   └── MobileLayout.tsx # 底部導航欄
│   ├── lib/                 # API 和工具函數
│   └── store/               # 狀態管理
├── public/                  # 靜態資源
├── ios/                     # iOS 原生專案 (執行 cap add ios 後生成)
├── android/                 # Android 原生專案 (執行 cap add android 後生成)
├── capacitor.config.ts      # Capacitor 配置
├── next.config.js           # Next.js 配置
└── package.json
```

## 🎨 移動端優化

相比 Web 版本，移動端做了以下優化：

1. **導航方式** - 使用底部 Tab Bar 替代頂部導航
2. **觸控優化** - 更大的按鈕和觸控區域
3. **手勢支持** - 下拉刷新、滑動操作
4. **推播通知** - 原生推播提醒
5. **離線功能** - 部分功能可離線使用

## 📲 發布到 App Store

### 前置需求

1. **Apple 開發者帳號** ($99/年)
2. **Mac 電腦** 和 **Xcode**
3. **App 圖標** (1024x1024)
4. **啟動畫面**
5. **App Store 截圖** (各種尺寸)

### 發布步驟

1. 在 Xcode 中配置 Bundle ID、版本號
2. 添加 App 圖標到 Assets.xcassets
3. 配置簽名和描述檔
4. Archive 專案
5. 上傳到 App Store Connect
6. 填寫 App 資訊、截圖
7. 提交審核

## 🔧 環境變數

創建 `.env.local` 文件：

```bash
NEXT_PUBLIC_API_URL=https://your-backend-url.com
```

## 📝 開發注意事項

1. **靜態導出** - Next.js 配置為靜態導出模式
2. **API 路徑** - 所有 API 都指向後端伺服器
3. **Hot Reload** - 開發時使用 `npm run dev`，不需要每次都同步到原生
4. **測試** - 先在瀏覽器測試，確認無誤後再同步到原生平台

## 🐛 常見問題

### Q: 為什麼 Xcode 打不開？
A: 確保已經執行 `npm run cap:add:ios` 來生成 iOS 專案

### Q: 修改代碼後沒有更新？
A: 需要重新構建並同步：`npm run build && npm run cap:sync`

### Q: App 無法連接後端？
A: 檢查 `.env.local` 中的 API URL 是否正確

### Q: 如何添加新的原生功能？
A: 使用 Capacitor 插件，例如：
```bash
npm install @capacitor/camera
npx cap sync
```

## 📚 相關資源

- [Capacitor 文檔](https://capacitorjs.com/docs)
- [Next.js 文檔](https://nextjs.org/docs)
- [iOS Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)

## 🙋‍♂️ 需要幫助？

如有問題，請參考主專案的 README 或提交 Issue。
